import { className } from "@/Attr"
import { div, p, render } from "@/Spinneret"
import { bem } from "@/Style"



main = () => {
  s = bem("App")
  render("app", <div className={s.e("core")}><p>Hey this is magical!</p></div>)
}
